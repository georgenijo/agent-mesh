# Archive

Completed, one-time artifacts kept for the historical record. Nothing here is
**load-bearing** — no live doc, prompt, or code references these paths. Moved
out of the active `docs/` tree to keep it navigable.

If you find yourself wanting to cite something in here from a live document,
that's a signal it doesn't belong in the archive — pull it back out.

| Item | What it was |
| --- | --- |
| `audit-hangar-brief.md` | One-time audit brief: should agent-mesh harvest from the sibling `hangar` project? |
| `audit-hangar-verdict.md` | The independent verdict on that brief (Rebuild-Core / Immediate-Harvest). |
| `stitch/` | A Google-Stitch UI design export (Material 3 color tokens + `code.html` + `screen.png`). Superseded by `web/` and the `mockups/` prototypes. |

**Still live, deliberately not archived:** `docs/spikes/` (feasibility evidence
referenced by `DECISIONS.md` and code comments), `docs/audit-multi-agent-pm.md`
(the source of several locked decisions), `docs/hardening-tracker.md` (an active
todo tracker), and every `docs/mockups/` file (cited across the guides and code).
